# P0 保护网基线（2026-09-19）

> 路线图 P0 交付物。用途: P1a 产物等价性对比、P2 构建时间对比、M 系列截图/退出 QA 的参照基线。
> 采集环境: Ubuntu / X11, 32 核, Python 3.14.4, CMake 4.2.3, 增量构建（未清缓存——FetchContent 已缓存）。

## 1. 构建命令与耗时（build.py 旧入口）

| 命令 | 耗时 (real) | 退出码 |
|---|---|---|
| `python3 build.py renderer` | 1m06.4s | 0 |
| `python3 build.py qt` | 18.8s（增量; user 时间 5m33s 并行摊到 32 核） | 0 |

实测调用参数（pgrep 捕获，供 preset 等价性核对）:
```
cmake -S renderer -B build/renderer_build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_POLICY_VERSION_MINIMUM=3.5
```

## 2. 测试全绿快照

| 组件 | 结果 | 备注 |
|---|---|---|
| controller_qt (`ctest --test-dir build/controller_qt`) | **41/41 通过, 0 失败** | 总时长 22.71s; REQUIRES_RENDERER 标签 4 项 11.55s |
| renderer (`ctest --test-dir build/renderer_build`) | **21/21 通过, 0 失败** | 总时长 0.04s |

## 3. build/bin 产物清单

清单与 sha256 见 `bin-sha256.txt`（本目录）。产物:
`FrameworkShaders/  Resources/  SampleShaders/  fonts/  libLive2DCubismCore.so  desktop-pet-controller-qt  desktop-pet-controller-qt-poc  desktop-pet-renderer  desktop-pet-renderer-vulkan  renderer_tests`

注: poc exe 为交付物直至 P2 退役（设计文档 B.1 拍板）。

## 4. 截图基线（--screenshot 模式）

- 命令: `QT_QPA_PLATFORM=xcb ./desktop-pet-controller-qt --screenshot --out <dir>`（自 build/bin 运行）
- 页面: welcome / monitor / settings（ScreenshotRunner 支持的全部页面）
- 退出码 0 ✓
- PNG 入库 `screenshots-p0/`，sha256:

| 文件 | sha256 |
|---|---|
| welcome.png | 3ca837ec...f219a |
| monitor.png | 43d254f2...db4 |
| settings.png | 2e95c4cc...5db |

完整哈希: `sha256sum screenshots-p0/*.png`。

## 5. 退出行为基线（exit QA，B.2 v2）

- screenshot 路径: 退出码 0；结束时出现 `QThreadStorage: entry N destroyed before end of thread` ×4 警告（与设计文档 B.2 记录的 teardown 债务一致——`std::exit()` 绕过析构的已知前科）
- 正常 GUI 退出路径: 本基线未覆盖（无显示交互自动化手段）——`--self-quit` 于 M4 实现后补
- 观察到的一次异常（记录待查）: 启动日志先 `WsServer: listening on 127.0.0.1:9001` 后紧接 `WsServer failed to listen on port 9001` 警告（可能为端口占用/双重监听日志瑕疵，M2 退出 QA 时复核）

## 6. 已知非阻塞噪声（基线期存在，非本次引入）

- `SettingRow.qml:49` binding loop ×5（settings 页截图时）
- TrayManager: GNOME 无 AppIndicator → tray 禁用（已知限制 R5）
- VoicePacks 目录不存在（未安装包，正常）
- COLD_START_MS=240（M 系列不劣化 >5% 的参照值: **≤252ms**）
