# 音频播放架构

> **实现状态**：✅ 渲染器侧已实现（`AudioManager`：miniaudio + libvorbis，OGG 播放）。`play_audio` / `stop_audio` / `set_volume` 指令已接入（错误码 7001/7002/7003）。控制器侧音频映射管理 / UI 待实现。
>
> 音频文件与模型文件**分离管理**，支持跨模型复用，降低存储占用。
> 渲染引擎模块概览参见 [渲染引擎设计](./README.md)，整体架构参见 [架构总览](../README.md)。
>
> **技术栈变更说明**：本文档原设计采用 OpenAL 作为渲染器侧音频后端。实际实现时改用 **miniaudio + libvorbis** 替代 OpenAL 方案——miniaudio 为单头文件库，集成更轻量，无需额外系统级依赖；libvorbis 负责 OGG/Vorbis 解码（CMake FetchContent 拉取）。下文中凡涉及 OpenAL 的描述均为原始设计，实际实现以 `renderer/src/AudioManager.cpp/.hpp` 为准。

---

## 一、音频管理设计

### 核心设计决策：音频与模型分离

传统方案将音频文件放在模型目录下（如 `models/Haru/sounds/`），导致：
- 相同音频在多个模型间重复存储，浪费磁盘空间
- 用户无法灵活组合不同模型与音频

本项目采用**音频独立管理**方案：

| 维度 | 传统方案（模型绑定） | 本项目方案（独立管理） |
| :--- | :--- | :--- |
| 存储位置 | 音频嵌入模型目录 | 音频存放在独立目录（`audio/`） |
| 存储占用 | 多模型共用音频时重复存储 | 同一音频文件仅存储一份 |
| 复用性 | 音频绑定单一模型 | 多个模型可引用同一音频文件 |
| 管理方式 | 由模型文件隐式关联 | 由控制面板通过映射配置显式管理 |
| 灵活性 | 用户无法自定义音频组合 | 用户可自由配置模型-动作-音频映射 |

### 音频类型

| 类型 | 说明 | 同步需求 | 管理方式 |
| :--- | :--- | :--- | :--- |
| **动作关联音频** | 与动作绑定的音效（如说话、点击反馈） | 高，需要与口型/表情精确同步 | 控制面板配置映射，渲染器按映射加载播放 |
| **独立音频** | 背景音乐、环境音效，与模型无直接关联 | 低，可独立播放 | 控制面板直接下发播放指令 |

---

## 二、职责分工

| 职责 | 控制面板 (Java) | 渲染器 (C++) |
| :--- | :--- | :--- |
| 音频文件导入/删除 | ✓ | ✗ |
| 音频映射配置（模型+动作 → 音频文件） | ✓ | ✗ |
| 音频映射下发 | ✓（设计预留 `set_audio_mapping` 指令，待实现） | ✓（设计：接收并缓存映射，待实现） |
| 音频文件加载与解码 | ✗ | ✅ 已实现（OGG / libvorbis） |
| 音画同步播放 | ✗ | ⚠️ 待实现（属 Phase 3c） |
| 指令式音频播放（play/stop） | ✗ | ✅ 已实现（`play_audio` / `stop_audio`） |
| 音量/静音控制 | ✓（UI 操作 → 下发指令，UI 待实现） | ✅ 已实现（响应 `set_volume` 指令） |

**音频由渲染器播放**的理由：

| 考量因素 | 渲染器播放 | 控制面板播放 |
| :--- | :--- | :--- |
| 音画同步 | ✓ 精确同步，无网络延迟 | ✗ 需要额外同步机制 |
| 动作触发 | ✓ 与动作状态机天然耦合 | ✗ 需额外传递时间戳 |
| 实现复杂度 | ✓ 直接调用音频 API | ✗ 同步逻辑复杂 |
| 音量控制 | ✓ 接收指令即可 | ✓ 天然支持 |

---

## 三、音频模块架构

```plain
┌─────────────────────────────────────────────────────────────┐
│                    控制面板 (Java)                           │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  音频管理 UI                                          │  │
│  │  - 音频文件导入/删除（FileChooser）                   │  │
│  │  - 音频映射配置（模型+动作组 → 音频文件路径）         │  │
│  │  - 音量滑块 / 静音开关                                │  │
│  └───────────────────────────────────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────┘
                           │ set_audio_mapping / set_volume /
                           │ play_audio 指令
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                 渲染引擎音频播放器 (C++)                     │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  音频映射缓存                                         │  │
│  │  - 接收控制面板下发的映射配置                         │  │
│  │  - 动作触发时查找映射，定位音频文件路径               │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  音频资源管理                                         │  │
│  │  - 从独立目录（audio/）加载音频文件                   │  │
│  │  - 缓存音频数据（LRU 策略）                          │  │
│  │  - 格式支持：OGG（libvorbis 解码，✅ 已实现）        │  │
│  │            （原设计为 WAV 原生 + MP3/OGG via dr_libs）│  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  播放控制                                             │  │
│  │  - 动作触发 → 查映射 → 自动播放关联音频              │  │
│  │  - 音画同步（时间戳对齐）                             │  │
│  │  - 多音轨混合                                         │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  音量管理                                             │  │
│  │  - 全局音量 / 音效音量（独立） / 静音状态             │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 四、实现状态明细

> 本节对照上方设计，标注各子模块在当前代码库中的落地情况，避免设计与实现混淆。

### 渲染器侧（`renderer/src/AudioManager.cpp/.hpp`）

| 设计模块 | 实现状态 | 说明 |
| :--- | :---: | :--- |
| 音频文件加载与解码（OGG） | ✅ 已实现 | miniaudio + libvorbis，`ManagedSound`（fileData + decoder + sound）管理活跃音效 |
| 播放控制（指令驱动） | ✅ 部分实现 | `play_audio` / `stop_audio` 指令已接入；动作触发→查映射→自动播放依赖映射缓存，**待实现** |
| 音量管理 | ✅ 已实现 | `set_volume` 指令 + `SetMuted`/`IsMuted`/`SetVolume`/`GetVolume` 接口 |
| 音频映射缓存（接收 `set_audio_mapping`） | ⚠️ 待实现 | 当前 19 条注册指令中**未包含** `set_audio_mapping`；渲染器尚无映射存储与自动播放逻辑 |
| 音画同步（时间戳对齐） | ⚠️ 待实现 | 属于 Phase 3c 口型同步范畴，当前未实现 |

### 控制器侧（Java）

| 设计模块 | 实现状态 | 说明 |
| :--- | :---: | :--- |
| 音频文件导入/删除 UI | ⚠️ 待实现 | JavaFX FileChooser 未接入 |
| 音频映射配置（模型+动作 → 音频） | ⚠️ 待实现 | 映射存储 / 下发流程未实现 |
| 音量滑块 / 静音开关 UI | ⚠️ 待实现 | 控制面板 UI 未实现 |

> **结论**：渲染器侧的**指令式音频播放**（任意 OGG 文件按需播放、停止、调音量）已完整可用。**动作关联自动播放**（依赖映射配置）与**控制器侧管理 UI** 仍待后续阶段实现。

