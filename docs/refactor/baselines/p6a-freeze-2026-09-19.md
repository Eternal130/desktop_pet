# P6a 冻结报告（2026-09-19）★架构定型点

> 提交: 1876f41 + aadb48d（打磨项）、**b446040（冻结提交）**。
> 评审: @oracle 独立冻结前评审（对照设计合同逐行核查 ABI 纪律/签名完整性/版本治理/README/门控逻辑）。

## 1. 冻结声明

自 `b446040` 起，`controller_qt/src/api/` 进入**版本化受控**:

- **API 版本: 1.0**（kApiMajor/Minor = 1/0）
- **iid: `org.desktop-pet.PanelPlugin/1.0`** 同步定版
- 变更规则 = README"冻结后变更规则（双轨）": 通用（加法升 minor，major bump 换新 iid）/ 阶段 1（可尾部追加虚方法）/ 阶段 2（vtable 与 PluginTypes 布局冻结，扩展走新接口新 iid）

## 2. 评审结论与处置

| 项 | 级别 | 处置 |
|---|---|---|
| E-1 DownloadRequest 合同文本（https+file://、sha256 大小写归一） | must-fix | ✅ b446040（改头文本，行为保留） |
| E-2 非 qrc qmlUrl 错误码（Generic→InvalidArgument，对齐头承诺） | must-fix | ✅ b446040（3 行代码前置校验） |
| E-3 版本治理双轨规则（原文本把阶段 1 便利冒充阶段 2 ABI 规则） | must-fix | ✅ b446040（README 重写） |
| E-5 阶段 2 所有权前提（宿主 create/delete 豁免仅阶段 1 成立; 第 5 条开启判据 = 工厂 destroy()） | should-fix | ✅ b446040（README 补记） |
| E-6 冻结合同头中的过期阶段叙事修剪 | should-fix | ✅ b446040 |
| E-7 组成表 stub 措辞去阶段化 | nice | ✅ b446040 |
| E-4 Failed 插件 instance 泄漏 + observer 留存（宿主行为债，不触 API 面） | should-fix | ⏸ **挂账**——建议 P6b 收尾时处理（本次暂停未做） |

评审五方面实质结论: 接口形态/ABI 纪律/门控逻辑/崩溃语义/能力诚实度**全部与合同一致且质量良好**（枚举固定底类型、fanout 级异常隔离、api_version 防 float 塌缩等细节到位）。

## 3. 打磨项验收（@observer 视觉）

- 插件页标题栏: **"示例插件 — Desktop Pet Controller"** ✓（重复 bug 已修，titleFor 机制 + 测试锁定）
- 插件管理卡片: 条目/版本/ID/STARTED pill/启用开关/重启提示齐全，零错误态，布局无破损 ✓
- 截图轮换新增 `--settings-anchor` 开发参数（锚点滚动定位，零正常运行时影响）
- 轻微观察（不阻塞）: 状态 pill 文字为英文 "STARTED"——若需中文可在 P6b 顺带本地化

## 4. 冻结时点状态

- 测试: **47/47** 全绿; 退出 QA EXIT=0; 截图 4 页（3 内置 + 1 插件页）零 QML 错误
- CI: 双平台 workflow 已入库（672e384），**待 push 首跑**
- 路线图: P0/P1a-d/P2/P3/P4/P5/P6a **全部完成**; P6b（平台下载器插件）按用户指示**暂停未启动**

## 5. P6b 就绪清单（恢复时直接开工）

API 1.0 冻结 + DownloadService 管线 + 插件页面 Loader + sdk-template 模板全部就绪。P6b = `plugins/downloader/`: 平台目录知识 + 搜索映射 + 下载编排 + 安装确认 UI（真 QML 页）。开放项: 目标平台的鉴权/CDN/分页怪癖未调研（设计文档 P6b 行已标注 4–6 开放至 6–10 人日）。顺带处理: E-4 宿主债 + STARTED 本地化 + emoji tofu 决策（矢量图标 vs Noto Color Emoji）。

> **更新（2026-09-20，存储布局修订）**: 语音包安装目录 / UAC 提权问题**已由本次改动解决**——用户包目录迁至 `%LOCALAPPDATA%\desktop-pet\VoicePacks\`（`ConfigDir::userVoicePacksDir()`），安装器改为按用户安装（`%LOCALAPPDATA%\DesktopPet` + `PrivilegesRequired=lowest`，无 UAC）。下载安装不再触碰安装目录，Program Files 只读场景不复存在。
